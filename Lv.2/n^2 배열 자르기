#solution
def solution(n, left, right):
    answer=[]
    for i in range(left,right+1):
        row=i//n
        column=i%n
        if row==column:
            answer.append(row+1)
        elif row>column:
            answer.append(row+1)
        else:
            answer.append(column+1)
    return answer
