#solution
def solution(targets):
    targets.sort(key=lambda x:x[1])
    stop=-1
    
    n=0
    
    for s,e in targets:
        if s>=stop:
            stop=e
            n+=1
            
    return n
