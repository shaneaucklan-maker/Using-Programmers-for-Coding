#solution
def solution(arr):
    arr.append(-1)
    a=[]
    
    for i in range(len(arr)):
        try:
            if arr[i]!=arr[i+1]:
                a.append(arr[i])
        except:
            IndexError
    return a
            

